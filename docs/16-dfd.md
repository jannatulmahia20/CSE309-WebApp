# Data Flow Diagram

User → Frontend (React) → Backend (FastAPI) → Database

Data flows from user input to backend API and is stored in database. The backend processes requests and sends responses back to the frontend for display.