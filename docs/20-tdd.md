# Technical Design Document

This system follows a client-server architecture. The frontend communicates with the backend using REST APIs. The backend handles business logic and database operations.

Authentication is implemented using JWT tokens.